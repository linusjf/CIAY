  for ((i = 1; i <= 4; i++)); do
    ifmt="$(printf "%03d" "$i")"
    fname="Day${ifmt}.jpg"
    rm "$fname"
  done
  for ((i = 6; i <= 31; i++)); do
    ifmt="$(printf "%03d" "$i")"
    fname="Day${ifmt}.jpg"
    rm "$fname"
  done
